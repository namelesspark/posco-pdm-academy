# =====================================================================
# AI 챌린지 2. 센서 노이즈 제거와 이동평균
# 리스트 데이터 처리 · 필터링 · 구간 슬라이싱
# 자세한 요구사항은 README.md 참고
# =====================================================================

raw = [23.1, 24.5, 999.0, 25.2, 24.8, -50.0, 26.1, 27.3, 25.9, 28.4, 26.7, 31.9]
LOW, HIGH = -10.0, 100.0   # 정상 범위 (이 밖은 센서 오류)
WINDOW = 3                 # 이동평균 구간 크기


# TODO 1. 정상 범위(`LOW` 이상 `HIGH` 이하) 안의 값만 `clean` 리스트에 모으기

# TODO 2. 범위 밖 값은 `removed` 리스트에 따로 모으고, 제거율을 % 로 출력 (소수 첫째 자리)

# TODO 3. `clean` 에서 연속 `WINDOW` 개씩 슬라이싱해 평균을 구해 `moving` 리스트 만들기 (소수 둘째 자리 반올림)

# TODO 4. `moving` 을 번호와 함께 한 줄씩 출력

# TODO 5. 연속한 이동평균의 **차이(절댓값)** 가 `1.0` 을 넘는 구간을 찾아 `N->N+1 구간 급변 (차이 X)` 형태로 출력

# TODO 6. `clean` 의 전체 평균, 최댓값, 최솟값 출력 (`sum`/`max`/`min` 사용 가능)


# 도전) `WINDOW` 를 `5` 로 바꿔도 코드가 그대로 동작하는지 확인해보기 (숫자를 하드코딩했다면 안 됨).
