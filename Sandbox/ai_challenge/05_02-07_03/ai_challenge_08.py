# =====================================================================
# AI 챌린지 8. 센서 통계 함수 세트 만들기
# 함수 정의 · 매개변수 · 반환값 · 여러 값 반환
# 자세한 요구사항은 README.md 참고
# =====================================================================

line_a = [78, 85, 92, 66, 88, 74]
line_b = [95, 97, 99, 101]
line_c = []          # 빈 리스트 - 이것도 처리돼야 함


# TODO 1. `get_average(values)` — 평균을 **return**. 빈 리스트면 `0` 을 돌려줄 것 (0으로 나누기 방지)

# TODO 2. `get_min_max(values)` — 최솟값과 최댓값 **두 개를 한 번에 return** (튜플로 묶여 나감)

# TODO 3. `count_over(values, limit)` — `limit` 초과 개수를 return

# TODO 4. `judge(avg)` — 평균을 받아 `위험`(90 이상) / `주의`(80 이상) / `정상` 문자열을 return

# TODO 5. 세 라인 각각에 대해 위 함수들을 호출하고, `get_min_max` 는 **언패킹으로 두 변수에 받기**

# TODO 6. `get_average` 의 반환값을 변수에 담아 `judge` 에 넘기는 **함수 조합**으로 최종 판정 출력

# TODO 7. 각 함수에 한 줄짜리 docstring 달기


# 도전) `judge` 에 기본값 인자를 추가해서 (`judge(avg, danger=90, warn=80)`) 기준선을 호출할 때 바꿀 수 있게 만들어보기.
