# =====================================================================
# AI 챌린지 9. 유연한 설비 판정 함수
# 기본값 인자 · 키워드 인자 · 지역변수
# 자세한 요구사항은 README.md 참고
# =====================================================================

records = [
    ("압축기_A", 88, {}),
    ("펌프_B",   95, {}),
    ("송풍기_C", 88, {"danger": 85}),
    ("절단기_D", 79, {"warn": 75, "unit": "bar"}),
]
# (설비명, 측정값, 덮어쓸 기준)


# TODO 1. `check(name, value, danger=90, warn=80, unit="도")` 함수 정의 — 뒤 세 개가 기본값 인자

# TODO 2. 함수 안에서 판정 결과와 판정 근거 문자열을 만들어 **두 값을 튜플로 return**

# TODO 3. `records` 를 순회하며, 덮어쓸 기준이 **비어 있으면 기본값으로**, 있으면 **키워드 인자로 덮어써서** 호출

# TODO 4. 반환값을 언패킹으로 받아 `설비명: 값단위 -> 판정 (근거)` 형태로 출력

# TODO 5. 같은 측정값 `88` 인데 `압축기_A` 와 `송풍기_C` 의 판정이 다른 이유를 주석으로 적기

# TODO 6. 함수 안에서 만든 지역변수를 함수 밖에서 출력하면 어떻게 되는지 주석으로 적기 (실제로 실행하진 말 것)


# 도전) `unit` 기본값을 활용해 압력 설비만 `bar`, 나머지는 `도` 로 나오게 하고, `unit` 만 키워드로 콕 집어 바꿔보기.
