# =====================================================================
# AI 챌린지 6. 측정값 대 임계값 초과율 분석
# 두 딕셔너리 연결 · zip · get 기본값
# 자세한 요구사항은 README.md 참고
# =====================================================================

names  = ["모터온도", "진동", "펌프압력", "전류", "유량"]
values = [95, 0.8, 88, 61, 47]

limits = {"모터온도": 90, "진동": 1.0, "펌프압력": 90, "전류": 50}
# 주의: "유량" 임계값이 없음


# TODO 1. `zip` 으로 `names` 와 `values` 를 묶어 `measured` 딕셔너리 만들기

# TODO 2. `items()` 로 순회하며 각 센서의 임계값을 `get` 으로 조회 — **없으면 건너뛰고** `임계값 미등록` 출력

# TODO 3. 초과율(`측정값 / 임계값 * 100`)을 소수 첫째 자리까지 구해 `센서명: 측정 X / 임계 Y -> Z%` 출력

# TODO 4. 초과율이 100 을 넘으면 `초과`, 90 이상이면 `임박`, 아니면 `정상` 판정을 같은 줄에 붙이기

# TODO 5. `초과` 판정 센서 이름만 모아 정렬 출력, 없으면 `초과 없음`

# TODO 6. 임계값이 등록된 센서들의 **평균 초과율** 출력 (소수 둘째 자리)


# 도전) 임계값이 없는 센서를 위해 `기본 임계값 = 100` 을 정해 두고, `get(name, 100)` 으로 처리해 전부 판정해보기.
