# =====================================================================
# AI 챌린지 5. 센서 카탈로그 관리 시스템
# 딕셔너리 · get · update · del · items
# 자세한 요구사항은 README.md 참고
# =====================================================================

sensors = {"모터온도": 78, "진동": 0.5, "펌프압력": 95, "유량": 42}
new_data = {"모터온도": 82, "전류": 61, "진동": 0.8}
broken = "유량"
query_list = ["모터온도", "회전속도", "전류", "습도"]


# TODO 1. 초기 딕셔너리의 센서 수, 키 목록, 값 목록 출력

# TODO 2. `update()` 로 `new_data` 를 한 번에 반영하고, **갱신된 키**와 **새로 추가된 키**를 각각 구분해 출력

# TODO 3. `del` 로 `broken` 센서를 삭제 (단, `in` 으로 존재를 먼저 확인할 것)

# TODO 4. `query_list` 의 센서를 하나씩 조회 — 있으면 값, 없으면 `미등록` 출력 (`get` 활용)

# TODO 5. `items()` 로 순회하며 값이 `60` 이상인 센서만 골라 `(값, 이름)` 튜플 리스트에 모으고 내림차순 정렬 출력

# TODO 6. 전체 값의 합계·평균 출력 (평균은 소수 둘째 자리)


# 도전) 삭제한 `유량` 센서를 다시 추가하고, `get` 의 기본값을 `0` 으로 줘서 미등록 센서까지 합계에 안전하게 포함시켜보기.
