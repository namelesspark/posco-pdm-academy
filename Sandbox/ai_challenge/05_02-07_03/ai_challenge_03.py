# =====================================================================
# AI 챌린지 3. 합성진동 기준 설비 랭킹
# 중첩 튜플 · 언패킹 · sorted 정렬 트릭
# 자세한 요구사항은 README.md 참고
# =====================================================================

sensors = [
    ("압축기_A", (3.0, 4.0), 62),
    ("펌프_B",   (1.5, 2.0), 71),
    ("송풍기_C", (6.0, 8.0), 55),
    ("절단기_D", (0.9, 1.2), 88),
    ("성형기_E", (5.0, 12.0), 47),
]
# (설비명, (진동X, 진동Y), 전류)


# TODO 1. `for` 언패킹으로 이름·진동튜플·전류를 꺼내고, 진동튜플을 다시 `x, y` 로 **한 번 더 언패킹**

# TODO 2. 합성진동을 계산해 `설비명 | X | Y | 합성 | 전류` 형태로 한 줄씩 출력 (합성은 소수 둘째 자리)

# TODO 3. `(합성진동, 설비명)` 튜플을 리스트에 모으고 `sorted(..., reverse=True)` 로 정렬

# TODO 4. 정렬 결과를 `N위: 설비명 - 합성진동` 으로 출력

# TODO 5. 합성진동 `5.0` 이상이면서 전류가 `60` 이상인 설비만 `이중위험` 으로 따로 출력

# TODO 6. 합성진동 1위 설비의 이름과 값을 콕 집어 출력 (정렬 결과의 0번 활용)


# 도전) 전류까지 포함해 `(합성진동, 전류, 설비명)` 3개짜리 튜플로 정렬해보기. 합성진동이 같을 때 전류로 순위가 갈리는지 확인.
