# =====================================================================
# AI 챌린지 7. 설비별 측정 이력 관리
# 중첩 딕셔너리 · 값에 리스트 담기
# 자세한 요구사항은 README.md 참고
# =====================================================================

plant = {
    "1번모터": {"상태": "정상", "이력": [78, 79, 81, 80]},
    "2번펌프": {"상태": "경고", "이력": [92, 95, 97, 99, 101]},
    "3번팬": {"상태": "정상", "이력": [55, 54, 56]},
    "4번압축기": {"상태": "경고", "이력": [88, 91]},
}


# TODO 1. `items()` 로 순회하며 설비마다 `설비명 [상태] 측정 N회` 출력

# TODO 2. 각 설비의 이력 평균·최대·최소를 구해 같은 블록에 출력 (평균은 소수 둘째 자리)

# TODO 3. 이력의 **마지막 값 - 첫 값** 으로 추세를 구해 `상승`/`하락`/`유지` 판정 출력

# TODO 4. 상태가 `경고` 인 설비 이름만 모아 정렬 출력

# TODO 5. **모든 설비의 모든 측정값**을 하나의 리스트로 합쳐 전체 평균과 총 측정 횟수 출력

# TODO 6. 평균이 가장 높은 설비를 찾아 `최고 평균 설비: 이름 (값)` 출력 (`(평균, 이름)` 튜플 정렬 활용)

# TODO 7. 대괄호를 연달아 써서 `2번펌프` 의 이력 **첫 값**과 **마지막 값**을 직접 꺼내 출력 (`plant[설비][항목][번호]`)

# TODO 8. `5번밸브` 가 등록돼 있는지 `not in` 으로 확인해 `미등록 설비` 출력


# 도전) 각 설비의 이력에서 **직전 값보다 오른 횟수**를 세어 `연속 상승 N회` 를 출력해보기.
