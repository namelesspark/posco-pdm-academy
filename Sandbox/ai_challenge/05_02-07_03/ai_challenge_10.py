# =====================================================================
# AI 챌린지 10. 스마트 정비 통합 리포트 (종합)
# 함수 + 딕셔너리 + 셋 + 튜플 + while + 리스트 처리 전부
# 자세한 요구사항은 README.md 참고
# =====================================================================

plant = {
    "압축기_A": [78, 85, 999, 88, 82],
    "펌프_B":   [95, 97, 99, 101, 98],
    "송풍기_C": [55, 54, -20, 56, 58],
    "절단기_D": [88, 91, 89, 90, 92],
    "성형기_E": [70, 68, 72, 71, 69],
}
yesterday_alerts = {"펌프_B", "성형기_E"}
VALID_LOW, VALID_HIGH = 0, 200
DANGER = 90


# TODO 1. `clean_values(values, low, high)` — 범위 밖 값을 제거한 **새 리스트**를 return

# TODO 2. `get_stats(values)` — `(평균, 최소, 최대)` **세 값을 한 번에** return, 빈 리스트면 `(0, 0, 0)`

# TODO 3. `judge(avg, danger=90)` — 기본값 인자를 쓴 판정 함수, `위험`/`정상` return

# TODO 4. 각 설비를 순회하며 정제 -> 통계 -> 판정하고, 제거된 이상치 개수도 함께 출력

# TODO 5. 평균이 `DANGER` 이상인 설비를 `today_alerts` **셋**에 모으기

# TODO 6. 셋 연산으로 `신규 이상`(오늘만), `지속 이상`(양쪽), `해소`(어제만) 각각 정렬 출력

# TODO 7. `(평균, 설비명)` 튜플 리스트로 평균 순위를 만들어 `while` 로 **상위 3개만** 출력

# TODO 8. 전체 설비 수·정상 수·위험 수와 위험 비율(%)을 f-string 으로 요약 출력


# 도전) 정제 후 값이 하나도 안 남은 설비가 있으면 `데이터 없음` 으로 따로 표시하고 통계에서 제외해보기.
